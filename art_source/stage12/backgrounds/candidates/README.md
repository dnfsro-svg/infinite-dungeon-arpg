# Retained background candidates

`candidate-manifest.json` is the machine-readable archival record for rejected and superseded ImageGen outputs. These images are retained for traceability only.

They are intentionally outside the formal ecology source directories. `background-sources.json`, `build_native_room_backgrounds.py`, runtime assets, and packaging must never consume `backgrounds/candidates/`.
