# Stage 7 被动星盘验收记录

日期：2026-07-14
范围：64 节点被动星盘的事务保存、确定性回放与回归验证。

## 自动化验收

- Debug：完整构建成功，`ctest --preset windows-msvc-debug` 为 27/27 通过。
- Release：完整构建成功，`ctest --preset windows-msvc-release` 为 27/27 通过。
- Dungeon：80 个用例通过；新增固定根种子的两个独立 session 各完成 1000 房间。每 25 房按 `8,9,10,22,23,24,36,37,38,50,51,52` 循环请求星盘节点；所有实际加点均经 `PendingSaveKind::passive_tree` 的 committed 回执完成。1000 条记录逐项一致，并且每条记录的房间种子、位图、提交代数、生命和护盾均来自已提交的 `session.snapshot()`。
- Persistence：38 个用例通过；在 8→9→10 路线上注入 `before_publish` 与 `after_publish`。重启后分别只恢复到完整的旧位图/点数，或完整的新位图/点数；没有出现点数与位图半提交。
- Platform：52 个用例通过；运行时覆盖 8→9→10、退款 10、保存完成及重启后的位图和点数保持。

## 窗口验收

主代理于 2026-07-14 以 Windows 自动化启动 Release `arpg_game.exe`，窗口可创建且进程保持响应，但捕获画面始终为纯白帧；发送 `P` 后也没有可观测的画面变化。因此无法诚实验证清房后打开覆盖层、点击 8/9/10 分配、退款 10、关闭覆盖层进入下一房，以及重启后的构筑保持。该项仍需在可正常采集 raylib/OpenGL 帧的 Windows 会话中人工完成。

## 已知限制

- 本阶段不包含装备、掉落、暴击、异常、穿透、伤害转换、资源系统或 Stage 8 内容。
- 自动化回归验证事务和状态一致性；当前 Windows 自动化会话的 raylib/OpenGL 画面采集为白帧，不能将其作为窗口交互已通过的证据。
